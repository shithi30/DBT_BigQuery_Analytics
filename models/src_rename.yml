sources:
  - name: proc
    dataset: first_dataset
    
    # freshness:
    #   warn_after: {count: 1, period: hour}
    #   error_after: {count: 3, period: hour}
    # loaded_at_field: Updated_at

    tables:
      - name: simtbl
        identifier: simweb_ubl
        columns:
          - name: platform
            tests:
              - not_null
              - string_not_empty